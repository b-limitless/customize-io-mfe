{
  "name": "@pasal/common-style",
  "version": "1.0.8",
  "description": "",
  "main": "index.js",
  "scripts": {
    "clean": "del ./dist/*",
    "build": "npm run clean && tsc styles/main.scss dist/main.css",
    "pub": "git add . && git commit -m \"Updates\" && npm version patch && npm run build && npm publish --access public"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "del-cli": "^5.0.0",
    "node-sass": "^9.0.0"
  }
}
