export const BASE_URI = 'https://pasal.dev'
export const API_PRIFIX = 'api';
export const REQUEST_BASE_URI = `${BASE_URI}/${API_PRIFIX}`;

export const APIS = {
    auth: {
        signup: `${REQUEST_BASE_URI}/user/singup`
    }, 
    user: {

    }, 
    product: {

    }
}