import React, { ReactNode } from 'react';
declare type Props = {
    children: ReactNode;
    rightIcon: ReactNode;
};
export default function DefaultTemplate({ children, rightIcon }: Props): React.JSX.Element;
export {};
