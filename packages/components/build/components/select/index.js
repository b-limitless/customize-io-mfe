import React from 'react';
import { Button } from '@mui/material';
export default function Select(_a) {
    var test = _a.test;
    return (React.createElement(React.Fragment, null,
        React.createElement(Button, null, "Hello World button")));
}
